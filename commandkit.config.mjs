import { defineConfig } from "commandkit";

export default defineConfig({
  src: "src", // The source directory of your project.
  main: "index.ts",
  watch: true, // The JavaScript entry point of your project.
});
